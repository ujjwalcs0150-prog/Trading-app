# Android build environment
# Set this to the IP address of the machine running the FastAPI backend.
# 10.0.2.2 is the Android Emulator's loopback alias for the host.
# For physical devices, use your LAN IP (e.g. 192.168.1.100).
VITE_API_URL=http://10.0.2.2:8000
